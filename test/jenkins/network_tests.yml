- hosts: localhost
  connection: local

  tasks:
  - name: get list of changed files
    shell: "git diff --name-only {{ lookup('env', 'GIT_PREVIOUS_SUCCESSFUL_COMMIT') }}"
    register: git_diff

  - debug: var=git_diff

  
